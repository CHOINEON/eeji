upload할때 Timestamp column 선택 
/upload 

모델 선택(밑에서 두번째)
epoch 맨 마지막

사용자가 모델 선택을 pls, rf => epoch은 비활성화

인이지만 


   <Form
        labelCol={{ span: 4 }}
        wrapperCol={{ span: 14 }}
        layout="horizontal"
        style={{ maxWidth: 600 }}
      >
        </Form>

else {
      messageApi.open({
        type: 'error',
        content: '업로드할 파일이 없습니다.',
        duration: 1,
        style: {
          margin: 'auto',
        },
      })
    }

3- 4 페이지 통합


1) 사용자한테 시간까지 입력받으면? 분까지

결측치 처리 안함/ 이상치 제거 안함 / 정규화 안함 옵션 추가(null)

type_missing : 지정값 채우기(userset) 선택 -> number_missing: number 들어옴

type_outlier: std 선택했을때 (0.1~3.5)
		perc => (1~49)


type_scaling: 선택안함

number_ma : 이동평균 정수로 넘어오기(1~10)
pls/ rfr이 아니면 
사용자가 모델 선택을 pls, rf => epoch, 베이지안 탐색횟수 비활성화


2) 전부 자동
일부 기능만 자동 옵션


manual auto (master)



upload할때 Timestamp column 선택 
/upload 

모델 선택(밑에서 두번째)
epoch 맨 마지막

사용자가 모델 선택을 pls, rf => epoch은 비활성화

인이지만 


   <Form
        labelCol={{ span: 4 }}
        wrapperCol={{ span: 14 }}
        layout="horizontal"
        style={{ maxWidth: 600 }}
      >
        </Form>

else {
      messageApi.open({
        type: 'error',
        content: '업로드할 파일이 없습니다.',
        duration: 1,
        style: {
          margin: 'auto',
        },
      })
    }


